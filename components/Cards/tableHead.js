import React from "react";
const TableHead = ({children})=>{
    return (
        <section className="card-container__table-head">
            {children}
        </section>
    )
}
export default TableHead;